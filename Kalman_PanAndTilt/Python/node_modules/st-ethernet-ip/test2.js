const eip = require('./dist/index.js')

let cont = new eip.Controller()

cont.connect('192.168.121.10')
    .then(async () => {
        let tag = cont.newTag('TestUDT2', 'MainProgram', false, 1)
        await cont.getTagArraySize(tag)
        
        await cont.readTag(tag)
        
        console.log(tag.value)
    })